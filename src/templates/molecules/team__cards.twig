{% for person in team %}
    <div class="team-card">
        <a  href="#" data-target="#team--{{ person.modalTarget }}" data-toggle="modal" >
            <div class="team-card__image">
                <img src="assets/team/{{ person.image }}" alt="{{ person.name }}">
            </div>
            <p class="team-card__name">{{ person.name }}</p>
            {#<p class="team-card__byline">{{ person.title }}</p>#}
            <p class="team-card__read-more">Read More</p>
        </a>
    </div>
{%  endfor %}