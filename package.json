{
  "name": "langan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "htmlhint": "^0.9.13"
  },
  "devDependencies": {
    "gulp": "github:gulpjs/gulp#4.0",
    "joes-gulp-tasks": "^0.4.0"
  },
  "scripts": {
    "precommit": "npm test",
    "test": "stylelint src/scss/**/*.scss",
    "start": "gulp",
    "gulp": "gulp --tasks && gulp",
    "gulp:tasks": "gulp --tasks",
    "build:prod": "gulp compile && cp -r src/assets/loaders build/css/images && cp -r src/assets/loaders build/images"
  },
  "author": "",
  "license": "ISC"
}
